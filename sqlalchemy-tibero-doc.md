# Sqlalchemy_tibero

TmaxData Tibero database 지원 버전

**Supported Tibero versions**

| Support type       | Versions   |
|--------------------|------------|
| Supported Version  | 7          |


## DBAPI Support

다음은 사용할 수 있는 Dialect/DBAPI입니다. 연결 정보는 각 DBAPI 섹션을 참고하시기 바랍니다.

1. [PyODBC](#tibero-pyodbc-dialect)

## Auto Increment Behavior

SQLAlchemy 테이블 객체에서 정수형 기본 키를 포함하는 경우, 일반적으로
“Auto Increment” 기능이 있다고 가정합니다. 즉, INSERT 시 자체적으로 기본 키 값을
생성할 수 있습니다. Tibero 에는 두 가지 옵션이 있으며, 이는
IDENTITY Column을 사용하거나 해당 Column에 SEQUENCE를 생성하는 것입니다.

### Specifying GENERATED AS IDENTITY

Tibero는 IDENTITY column을 사용하여Auto Increment 동작을 지정할 수 있습니다

```python
t = Table('mytable', metadata,
    Column('id', Integer, Identity(), primary_key=True),
    Column(...), ...
)
```

위의 테이블 객체에 대한 CREATE TABLE 문은 다음과 같습니다:

```sql
CREATE TABLE mytable (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY,
    ...,
    PRIMARY KEY (id)
)
```

Tibero에서 `Identity` 객체는 초기 값, 증가 값 옵션을 설정할 수 없고,
각각 기본 값으로 1을 가집니다. 해당 옵션은 SEQUENCE를 통해 사용할 수 있습니다.
Tibero 는 표준 옵션 외에도 `Identity.always`를 `None` 으로 설정하여 
기본 생성 모드를 사용할 수 있으며, DDL 상에서 GENERATED AS IDENTITY로 변환됩니다.

Tibero는 Identity 컬럼 생성 시 on_null 옵션을 지원합니다:

* `on_null`:  `True`로 설정되면 'BY DEFAULT' identity 컬럼과 함께
  `ON NULL` 키워드가 추가됩니다. always 옵션을 사용하지 않거나 False 여야 합니다:
  ```python
  t = Table('mytable', metadata,
      Column('id', Integer, Identity(always=False, on_null=True), primary_key=True),
      Column(...), ...
  )
  ```

### Using a SEQUENCE

Tibero는 SEQUENCE를 지원하며, SQLAlchemy는 SEQUENCE를 통해 autoincrement 기능을 지원합니다.
SEQUENCE를 통해 autoincrement 기능을 사용하는 경우 대부분의 문서 예제와 다를 수 있습니다.
SEQUENCE를 지정하려면 Column 구문에 전달되는 sqlalchemy.schema.Sequence 객체를 사용하세요:

```default
t = Table('mytable', metadata,
      Column('id', Integer, Sequence('id_seq', start=1), primary_key=True),
      Column(...), ...
)
```

테이블 리플렉션을 사용하는 경우에도 이와 같이 명시해주어야 합니다:

```default
t = Table('mytable', metadata,
      Column('id', Integer, Sequence('id_seq', start=1), primary_key=True),
      autoload_with=engine
)
```

#### 주의
하지만 sqlalchemy를 통해 정의된 테이블을 autoload_with로 다시 불러올 때,
데이터 타입이 같지 않을 수 있습니다. 이는 테이블을 저장할 때 일부 메타데이터가 손실되기 때문입니다.
예를 들어, Float 타입은 autoloading 과정에서 Number 타입으로 불러오게 됩니다. 따라서, 이러한
이유로 autoload_with를 사용하는 것을 권장하지 않습니다. 데이터 타입이나 기타 메타데이터가 일관성
있게 유지되지 않을 수 있기 때문에, 테이블 정의 시 명시적으로 각 열의 타입과 속성을 지정하는 것이
더 안전합니다. 특히, SEQUENCE나 정밀한 타입 제어가 필요한 경우에는 autoload_with 대신 
명시적으로 테이블을 정의하고 관리하는 것이 좋습니다.


## Transaction Isolation Level / Autocommit

Tibero 는 READ COMMITTED, SERIALIZABLE, AUTOCOMMIT 모드를 제공합니다.

연결 별로 실행 옵션을 설정하려면:

```default
connection = engine.connect()
connection = connection.execution_options(
    isolation_level="AUTOCOMMIT"
)
```

READ COMMITTED와 SERIALIZABLE의 경우, Tibero Dialect는 세션 레벨에서 ALTER SESSION을
사용하여 트랜잭션 격리 수준을 설정하며, 연결이 연결 풀로 반환될 때 기본 설정으로 복원됩니다

`isolation_level` 의 유효한 값은 다음과 같습니다:

* `READ COMMITTED`
* `AUTOCOMMIT`
* `SERIALIZABLE`


## Identifier Casing

Tibero에서 Data Dictionary는 대문자 텍스트를 사용하여 대소문자를 구분하지 않는 모든 식별자
이름을 나타냅니다. 반면에 SQLAlchemy는 모두 소문자로 구성된 식별자 이름을 대소문자를 구분하지
않는 것으로 간주합니다. SQLAlchemy 측에서 대문자 이름을 사용하면 대소문자를 구분하는 식별자를
나타내고 SQLAlchemy는 이름을 인용합니다. 이로 인해 Tibero 에서 받은 Data Dictionary의
data와 불일치가 발생하므로 식별자 이름이 실제로 대소문자를 구분하여 생성되지 않는 한
(예시: 인용된 이름 사용) , SQLAlchemy 측에서는 모든 소문자 이름을 사용해야 합니다.

## Max Identifier Lengths

Tibero의 기본 최대 식별자 길이는 128입니다. 이 값은 특히 제약 조건 명명 규칙이 있는 경우 생성된
SQL 레이블 이름 및 제약 조건 이름 생성 영역에서 SQLAlchemy에 영향을 미칩니다. 제약 조건 명명
규칙 구성에 설명된 기능이 사용되고 있습니다.

`create_engine.max_identifier_length` 매개 변수를 설정하면 이 변경이
무시되며, 제공된 값이 그대로 사용됩니다:

```python
engine = create_engine(
    "tibero+pyodbc://tibero:tmax@Tibero",
    max_identifier_length=30)
```

최대 식별자 길이는 SELECT 문에서 익명화된 SQL 레이블을 생성할 때 적용되지만 명명 규칙에서 제약 조건
이름을 생성할 때 더욱 중요합니다. SQLAlchemy 가 이 기본값을 보수적으로 변경해야 하는 필요성이 발생한
것은 바로 이 영역입니다. 예를 들어, 다음 명명 규칙은 식별자 길이에 따라 매우 다른 두 가지 제약 조건
이름을 생성합니다:

```python
from sqlalchemy import Column
from sqlalchemy import Index
from sqlalchemy import Integer
from sqlalchemy import MetaData
from sqlalchemy import Table
from sqlalchemy.schema import CreateIndex

m = MetaData(naming_convention={"ix": "ix_%(column_0N_name)s"})

t = Table(
    "t",
    m,
    Column("some_column_name_1", Integer),
    Column("some_column_name_2", Integer),
    Column("some_column_name_3", Integer),
)

ix = Index(
    None,
    t.c.some_column_name_1,
    t.c.some_column_name_2,
    t.c.some_column_name_3,
)

tibero_dialect = dialect(max_identifier_length=30)
print(CreateIndex(ix).compile(dialect=tibero_dialect))
```

식별자 길이가 30일 때, 위의 CREATE INDEX 구문은 다음과 같이 생성됩니다:

```sql
CREATE INDEX ix_some_column_name_1s_70cd ON t
(some_column_name_1, some_column_name_2, some_column_name_3)
```

그러나 길이가 128일 때, 다음과 같이 변경됩니다:

```sql
CREATE INDEX ix_some_column_name_1some_column_name_2some_column_name_3 ON t
(some_column_name_1, some_column_name_2, some_column_name_3)
```

## LIMIT/OFFSET/FETCH Support

Tibero는 Select.limit() 및 Select.offset() 메서드에서 
FETCH FIRST N ROW / OFFSET N ROWS 구문을 사용합니다. 
이 구문은 Select.fetch() 메서드를 사용하여 직접 사용할 수도 있습니다. 
UNION 과 같은 복합문 내에서 LIMIT/OFFSET을 사용할 경우, 윈도우 함수를 사용하는
에뮬레이션 방식이 사용됩니다. 이 방식은 ROW_NUMBER를 사용한 서브쿼리를 생성하게 되며, 
이는 성능 문제를 일으킬 수 있습니다.

`enable_offset_fetch=False` 파라미터를 통해 윈도우 함수 사용을 강제할 수 있습니다.
또한, `enable_offset_fetch=False`일 때 성능 최적화를 위해
`optimize_limits=True` 로 FIRST_ROWS() 최적화 키워드를 활성화할 수 있습니다.

LIMIT/OFFSET 값은 “post compile” 방식으로 처리되며, SQL 실행 시 커서에 
전달되기 전에 할당됩니다.

## RETURNING Support

Tibero 데이터베이스는 INSERT, UPDATE, DELETE 문에 대해 RETURNING 구문을 완전히
지원합니다. 이는 단일 컬렉션의 바인딩된 매개변수와 함께 호출된 경우에 해당하며, 예를 들어
cursor.execute() 스타일의 문장에서 사용됩니다. (SQLAlchemy는 일반적으로 executemany
문에서 RETURNING을 지원하지 않습니다.) 또한 여러 행도 반환될 수 있습니다.

## ON UPDATE CASCADE

Tibero 는 ON UPDATE CASCADE 기능을 지원하지 않습니다.
SQLAlchemy ORM을 사용할 때, ORM은 수동으로 연쇄 업데이트(cascading updates)를 수행하는 데
제한이 있습니다.

## ForeignKey

Tibero 데이터베이스는 Foreign Key에 deferrable option을 사용할 없습니다. 따라서
relationship()에 대해 `deferrable`과 `initially` 옵션 사용이 불가합니다.


## Synonym/DBLINK Reflection

아직 지원하지 않는 기능입니다. 추후 지원 예정입니다.

## Constraint Reflection

Tibero Dialect 는 테이블의 인덱스뿐만 아니라 외래 키, 고유 키 및 CHECK constraints
정보를 반환할 수 있습니다.

이러한 제약 조건에 관한 raw data는 다음 함수를 통해 얻을 수 있습니다.

```python
Inspector.get_foreign_keys()
Inspector.get_unique_constraints()
Inspector.get_check_constraints()
Inspector.get_indexes()
```

`Table` 레벨에서 리플렉션을 사용하는 경우, `Table` 에는 이러한 제약 조건도 포함됩니다.

다음 사항에 유의하세요:

* `Inspector.get_check_constraints()` 메소드를 사용할 때 기본적으로 `NOT NULL`을
  제약 조건을 가진 열에 대한 정보를 반환하지 하지 않습니다. ``NOT NULL` 제약 조건을
  포함한 열까지 반환할려면 `include_all=True` 플래그를 사용해주세요:
  
  ```python
  from sqlalchemy import create_engine, inspect

  engine = create_engine("tibero+pyodbc://tibero:tmax@Tibero7")
  inspector = inspect(engine)
  all_check_constraints = inspector.get_check_constraints("some_table", include_all=True)
  ```

* Tibero는 테이블의 primary key 에 대한 암시적 인덱스를 생성합니다. 이 인덱스는
  모든 인덱스 결과에서 제외됩니다.

* Tibero Dialect는 index나 constraint에 대한 정보를 얻어올 때 경우에 따라 정확한 데이터를 얻어 못하는 
하는 버그가 있습니다. 추후 수정할 예정이며 Reflection을 사용해야 한다면 주의해주십시오. 

## Table names with SYSTEM/SYSAUX tablespaces

`Inspector.get_table_names()` 및 `Inspector.get_temp_table_names()`
메서드는 각각 현재 엔진에 대한 테이블 이름 목록을 반환합니다. 이 메서드들은 기본값으로
`SYSTEM` 및 `SYSAUX` tablespaces가 작업에서 제외됩니다. 기본값을 변경하려면 engine 객체를 생성할 때
`exclude_tablespaces` 매개변수를 사용하여 엔진 수준에서 변경할 수 있습니다:

```python
# exclude SYSAUX and SOME_TABLESPACE, but not SYSTEM
e = create_engine(
  "tibero+pyodbc://tibero:tmax@Tibero7",
  exclude_tablespaces=["SYSAUX", "SOME_TABLESPACE"])
```

## DateTime Compatibility

Tibero 에는 `DATETIME` 이라는 data type이 없으며 대신 실제로 날짜 및 시간 값을 저장할 수 있는
`DATE` 가 있습니다. 이러한 이유로 Tibero Dialect는 SQLAlchemy `DateTime` 의 자식 클래스인
`DATE` 클래스를 제공합니다. 이 자식 클래스는 특별한 로직을 가지고 있지 않은 marker로서 Tibero 데이터베이스
`Date` 타입을 지원하기 위해 존재합니다.

## Tibero Table Options

Tibero에서 `CREATE TABLE` 구문을 사용할 때, 테이블의 구조와 함께 다양한 옵션을 설정할 수 있습니다. 이 섹션에서는
Tibero가 지원하는 테이블 관련 주요 옵션에 대해 설명하고, 예시와 함께 구체적인 사용 방법을 안내합니다.

### 1. `ON COMMIT`

Tibero는 글로벌 임시 테이블을 지원하며, 트랜잭션이 종료될 때 테이블의 데이터를 어떻게 처리할지 결정할 수 있는 `ON COMMIT` 옵션을
제공합니다. 이 옵션을 설정하면, 트랜잭션이 커밋될 때 테이블의 데이터가 유지될지 아니면 삭제될지를 정의할 수 있습니다.

- **예시**: 임시 테이블의 행을 커밋 후에도 유지하는 경우
  ```python
  Table(
      "some_table", metadata, 
      Column('id', Integer, primary_key=True),
      prefixes=['GLOBAL TEMPORARY'], 
      tibero_on_commit='PRESERVE ROWS'
  )
  ```

- `tibero_on_commit='PRESERVE ROWS'`: 트랜잭션 커밋 후에도 테이블의 행이 유지됨.
- `tibero_on_commit='DELETE ROWS'`: 트랜잭션 커밋 후 테이블의 행이 삭제됨.

### 2. `COMPRESS`

Tibero는 테이블의 데이터 압축을 지원하며, `COMPRESS` 옵션을 통해 테이블 또는 특정 열의 데이터를 압축할 수 있습니다.
압축은 저장 공간을 줄이고, 성능을 최적화하는 데 유리합니다. Tibero는 여러 압축 모드를 제공하며, 이를 통해 다양한 작업
환경에 맞는 압축을 설정할 수 있습니다.

- **예시**: 테이블 또는 열에 대한 기본 압축 적용
  ```python
  Table(
      'mytable', metadata, 
      Column('data', String(32)),
      tibero_compress=True  # COMPRESS BASIC 모드 적용
  )
  ```

- **예시**: 특정 압축 모드 적용 (OLTP 모드)
  ```python
  Table(
      'mytable', metadata, 
      Column('data', String(32)),
      tibero_compress='OLTP'  # COMPRESS FOR OLTP 모드 적용
  )
  ```

#### `tibero_compress` 파라미터 설명

- **`tibero_compress=True`**: `bool` 타입으로, `COMPRESS BASIC` 옵션을 적용합니다.
  
- **`tibero_compress=<arg>`**: `str` 타입으로, `COMPRESS FOR <arg>` 형태의 특정 압축 모드를
적용할 수 있습니다. 예를 들어, OLTP 환경에서는 트랜잭션 성능을 고려한 `COMPRESS FOR OLTP`를 사용할
수 있습니다. 이 외에도 티베로가 새 기능을 추가하면 따라 다른 압축 모드를 지정할 수 있습니다.


## Tibero Specific Index Options

Tibero는 다양한 인덱스 옵션을 지원하여 데이터베이스 성능을 최적화할 수 있습니다. 이 섹션에서는 Tibero에서 지원하는
**Bitmap Indexes**와 **Index Compression**에 대해 설명합니다.

### 1. **Bitmap Indexes**

Tibero는 **Bitmap 인덱스**를 제공하여, 선택도가 낮은 컬럼(즉, 중복된 값이 많은 컬럼)에 대해 효율적으로 인덱싱을
할 수 있습니다. Bitmap 인덱스는 B-tree 인덱스와 달리 대용량 데이터에 대해 공간을 절약하고, 빠른 읽기 성능을 제공합니다.
Bitmap 인덱스를 생성하려면 `tibero_bitmap` 매개변수를 설정합니다.

- **예시**: Bitmap 인덱스 생성
  ```python
  Index('my_index', my_table.c.data, tibero_bitmap=True)
  ```

- **주의 사항**: 
  - Bitmap 인덱스는 **UNIQUE** 인덱스로 설정할 수 없으며, **압축(Compression)**을 지원하지 않습니다.
  - SQLAlchemy는 이러한 제약 사항을 확인하지 않으므로, 제약 사항은 Tibero DB에서 직접 확인됩니다.

### 2. **Index Compression**

Tibero는 **반복되는 값**이 많이 포함된 인덱스에 대해 효율적인 저장 공간을 제공하기 위해 **인덱스 압축**을 지원합니다.
`tibero_compress` 매개변수를 통해 인덱스에서 키 압축을 활성화할 수 있으며, 이는 특히 중복 값이 많은 컬럼들에 대해 인덱스를
생성할 때 유용합니다.

- **예시**: 기본 인덱스 압축 활성화
  ```python
  Index('my_index', my_table.c.data, tibero_compress=True)
  ```

- **예시**: 특정 열에 대해 압축 적용
  ```python
  Index('my_index', my_table.c.data1, my_table.c.data2, unique=True, tibero_compress=1)
  ```

- **설명**:
  - `tibero_compress=True`: 압축을 활성화하며, 기본적으로 UNIQUE 인덱스가 아닌 경우 **모든 컬럼**에 대해 압축을 적용합니다.
  UNIQUE 인덱스인 경우 **마지막 열을 제외한 모든 컬럼**에 대해 압축을 적용합니다.
  - `tibero_compress=1`: 정수 값을 지정하면, 인덱스의 앞에서부터 해당 값만큼의 컬럼을 압축하게 됩니다. 위 예시에서는 첫 번째
  컬럼에 대해서만 압축이 적용됩니다.

#### 주의 사항:
1. `tibero_compress` 옵션은 Tibero가 제공하는 고유한 기능으로, SQLAlchemy에서 이를 직접적으로 확인하지는 않습니다.
잘못된 인덱스 구성으로 인해 오류가 발생할 수 있으므로, DB에서 실제로 동작하는 제약 사항을 염두에 두고 사용해야 합니다.

## Tibero Data Types

Tibero 에서 사용 가능한 대문자 데이터 타입은 Sqlalchemy의 dialect 에서 직접 가져올 수 있습니다.

```python
from sqlalchemy_tibero import (
    BLOB,
    CHAR,
    CLOB,
    DATE,
    DOUBLE_PRECISION,
    FLOAT,
    INTERVAL,
    LONG,
    NCLOB,
    NCHAR,
    NUMBER,
    NVARCHAR,
    NVARCHAR2,
    RAW,
    TIMESTAMP,
    VARCHAR,
    VARCHAR2,
)
```


다음은 Tibero 에만 적용되는 데이터 타입이나 Tibero에서 특별한 생성 인자를 요구하는
타입입니다:

| Object Name                            | Description                           |
|----------------------------------------|---------------------------------------|
| [BFILE](#)                             | 차후 지원 고려중                             |
| [BINARY_DOUBLE](#binary_double-데이터-타입) | CREATE, INSERT만 가능, SELECT 지원 예정      |
| [BINARY_FLOAT](#binary_double-데이터-타입)  | CREATE, INSERT만 가능, SELECT 지원 예정      |
| [DATE](#date-데이터-타입)                   | Tibero DATE type                      |
| [FLOAT](#float-데이터-타입)                 | Tibero FLOAT type                     |
| [INTERVAL](#interval-데이터-타입)           |                                       |
| [LONG](#long-데이터-타입)                   |                                       |
| [NCLOB](#nclob-데이터-타입)                 |                                       |
| [NUMBER](#number-데이터-타입)               |                                       |
| [RAW](#raw-데이터-타입)                     |                                       |
| [ROWID](#rowid-데이터-타입)                 | Tibero ROWID 타입                       |
| [TIMESTAMP](#timestamp-데이터-타입)         | TIMESTAMP WITH \[LOCAL\] TIME ZONE 지원 |

**주의**: 현재 **BFILE**, **LONGRAW**, **XMLTYPE**, **JSON** 데이터 타입은 Tibero에서 지원되지 않습니다.

---

### BINARY_DOUBLE 데이터 타입

`sqlalchemy_tibero.BINARY_DOUBLE` 클래스는 Tibero에서 사용되는 이중 정밀도 부동 소수점 숫자를 나타냅니다. 이 데이터 타입은 주로 수학적 계산이나 정밀한 수치 데이터를 처리하는 데 사용됩니다.

**사용법**:

```python
import sqlalchemy_tibero as tibero
from sqlalchemy_tibero import BINARY_DOUBLE
from sqlalchemy import Column
from sqlalchemy import Float

column = Column('my_double_column', BINARY_DOUBLE(precision=15))

# SQLAlchemy.Float.precision 매개변수는 DDL을 렌더링할 때 허용되지 않습니다.
# TIbero은 소수 자리 수로 지정된 부동 소수점 정밀도를 지원하지 않기 때문입니다.
# 대신, Tibero 전용 FLOAT 데이터 타입을 사용하고 FLOAT.binary_precision 매개변수를
# 지정해야 합니다
Column(
    "float_data",
    Float(5).with_variant(tibero.FLOAT(binary_precision=16), "tibero")
)
```

**매개변수 설명**:

- `precision`: 숫자가 가질 수 있는 최대 자릿수를 설정합니다. 기본값은 `None`입니다.
- `asdecimal`: 이 값이 `True`로 설정되면 부동 소수점 값이 Decimal로 반환됩니다. 기본값은 `False`입니다.
- `decimal_return_scale`: Decimal 값을 반환할 때의 소수점 이하 자리수를 설정합니다. 기본값은 `None`입니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.BINARY_DOUBLE(precision: int | None = None, asdecimal: bool = False, decimal_return_scale: int | None = None)
```

---

### DATE 데이터 타입

`sqlalchemy_tibero.tibero.DATE` 클래스는 Tibero 데이터베이스에서 사용되는 DATE 타입을 나타냅니다. 이 데이터 타입은 날짜와 시간 정보를 모두 저장할 수 있습니다. 

#### 기본 사용법

Tibero의 DATE 타입을 사용하여 테이블을 정의할 수 있습니다. 예를 들어, 다음과 같이 `event_date`라는 DATE 컬럼을 포함한 테이블을 만들 수 있습니다:

```python
from sqlalchemy import create_engine, Column, Integer, Table, MetaData
from sqlalchemy_tibero import DATE

engine = create_engine('tibero+pyodbc://tibero:tmax@Tibero7')

metadata = MetaData()

my_table = Table(
    'my_table',
    metadata,
    Column('id', Integer, primary_key=True),
    Column('event_date', DATE)
)

metadata.create_all(engine)
```

#### 주의사항 지원

Tibero DATE 클래스는 SQLAlchemy DateTime의 자식 클래스이기 떄문에 `timezone=True` 매개 변수를
설정할 수 있습니다. 하지만 Tibero의 DATE 데이터 타입은 날짜와 시간을 저장할 수 있지만, 타임존 정보는 지원하지
않습니다. 따라서 DATE 타입을 사용할 때 timezone=True로 설정해도 타임존 정보는 저장되지 않기 때문에
실질적으로는 의미가 없습니다. 타임존 지원이 필요한 경우에는 TIMESTAMP 데이터 타입은 날짜와 시간 정보를
포함하며 타임존 정보를 처리할 수 있습니다. 따라서 타임존 지원이 필요한 경우에는 TIMESTAMP 타입을 사용하는
것이 좋습니다.


---

### FLOAT 데이터 타입

`sqlalchemy_tibero.FLOAT` 클래스는 Tibero 데이터베이스에서 사용되는 FLOAT 타입을 나타냅니다.
binary precision 매개변수를 통해 정밀도를 조절할 수 있습니다.

**사용법**:

```python
from sqlalchemy import Column, Float
from sqlalchemy_tibero import FLOAT

# binary precision을 64로 설정하여 FLOAT 타입 컬럼을 정의
column = Column('my_float_column', FLOAT(binary_precision=64))

# SQLAlchemy.Float.precision 매개변수는 DDL에서 허용되지 않으며,
# Tibero의 FLOAT는 binary precision를 사용합니다. 이를 위해 with_variant()를 사용하여 
# 특정 데이터베이스에 맞게 변형된 타입을 지정할 수 있습니다.
Column(
    "float_data",
    Float(5).with_variant(FLOAT(binary_precision=16), "tibero")
)
```

**매개변수 설명**:

- `binary_precision`: 기본값은 126이며, 이는 약 38자리 소수점 정밀도와 비슷합니다.
- `asdecimal`: 반환값을 `Decimal`로 반환할지 여부를 결정합니다. 기본값은 `False`입니다.
- `decimal_return_scale`: `Decimal` 값을 반환할 때의 사용할 소수점 이하 자리수를 설정합니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.FLOAT(binary_precision: int | None = None, asdecimal: bool = False, decimal_return_scale: int | None = None)
```

---

### INTERVAL 데이터 타입

`sqlalchemy_tibero.INTERVAL` 클래스는 Tibero의 `INTERVAL` 타입을 나타내며, 주로 날짜 및 시간을
간격으로 나타낼 때 사용됩니다. 현재 sqlalchemy_tibero에서는 **DAY TO SECOND** 형식의 `INTERVAL`만
지원합니다. `YEAR TO MONTH` 형식 지원은 아직 지원하지 않습니다.

**사용법**:

```python
from sqlalchemy import Column
from sqlalchemy_tibero import INTERVAL

column = Column('my_interval_column', INTERVAL(day_precision=3, second_precision=5))
```

**매개변수 설명**:

- `day_precision`: `INTERVAL`에서 **day** 필드에 저장할 자릿수를 설정합니다. 기본값은 `2`이며, 최대
9자리까지 설정할 수 있습니다.
- `second_precision`: **fractional seconds** 필드에 저장할 소수점 이하 자릿수를 설정합니다. 기본값은
`6`이며, 최대 9자리까지 설정할 수 있습니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.INTERVAL(day_precision: int | None = None, second_precision: int | None = None)
```

### NCLOB 데이터 타입

`sqlalchemy_tibero.NCLOB` 클래스는 Tibero 데이터베이스에서 **NCLOB**
(National Character Large Object) 타입을 나타냅니다. 이 데이터 타입은 주로 다국어 텍스트나 매우
큰 문자열 데이터를 저장할 때 사용됩니다. NCLOB은 UTF-8과 같은 다국어 문자를 저장할 수 있는 기능을 제공합니다.

**사용법**:

```python
from sqlalchemy import Column
from sqlalchemy_tibero import NCLOB

column = Column('my_nclob_column', NCLOB())
```


**참고사항**:
대부분의 경우, 비-ASCII 데이터를 저장해야 할 때는 `Unicode` 또는 `UnicodeText` 데이터 타입을 사용하는
것이 좋습니다. 이 타입들은 데이터베이스에서 적절한 데이터 타입을 사용할 수 있도록 보장합니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.NCLOB()
```

---

### NVARCHAR2 데이터 타입

`sqlalchemy_tibero.NVARCHAR2`는 `sqlalchemy.NVARCHAR` 타입의 별칭(alias)입니다.

**사용법**:

```python
from sqlalchemy import Column
from sqlalchemy_tibero import NVARCHAR2

column = Column('my_nvarchar_column', NVARCHAR2(length=100))
```

**매개변수 설명**:

- `length`: 컬럼의 최대 문자 길이를 설정합니다. 이 값은 DDL (데이터 정의 언어) 생성 시 사용되며,
길이를 지정하지 않으면 기본값으로 처리됩니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.NVARCHAR2(length: int | None = None)
``` 

---

### NUMBER 데이터 타입

`sqlalchemy_tibero.NUMBER`는 Tibero 데이터베이스의 `NUMBER` 타입을 나타내며, 숫자 데이터를 저장하는
데 사용됩니다. 이 타입은 매우 유연하여 정수와 소수인 숫자 모두를 처리할 수 있습니다. `NUMBER` 클래스는
`Numeric`과 `Integer` 타입을 모두 상속하여 다양한 숫자 데이터 형식을 지원합니다.

**사용법**:

```python
from sqlalchemy import Column
from sqlalchemy_tibero import NUMBER

column = Column('my_number_column', NUMBER(precision=10, scale=2))
```

**매개변수 설명**:

- `precision`: 저장될 수 있는 최대 자릿수를 나타냅니다. 기본값은 Tibero 데이터베이스의 설정을 따릅니다.
- `scale`: 소수점 이하 자리수를 설정합니다. 정수일 경우 `scale=0`으로 설정합니다.
- `asdecimal`: 반환값을 `Decimal`로 반환할지 여부를 결정합니다. 기본값은 `None`입니다. `None`일 때
dbapi에 따라 반환값이 다릅니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.NUMBER(precision: int | None = None, scale: int | None = None, asdecimal: bool | None = None)
```

---

### LONG 데이터 타입

`sqlalchemy_tibero.LONG`는 Tibero 데이터베이스의 `LONG` 타입을 나타냅니다.

**사용법**:

```python
from sqlalchemy import Column
from sqlalchemy_tibero import LONG

column = Column('my_long_column', LONG)
```

**클래스 시그니처**:
```python
class sqlalchemy_tibero.LONG()
```

**참고**: 

대부분의 경우, 비-ASCII 데이터를 저장해야 할 때는 `Unicode` 또는 `UnicodeText` 데이터 타입을 사용하는
것이 좋습니다. 이 타입들은 데이터베이스에서 적절한 데이터 타입을 사용할 수 있도록 보장합니다.

---

### RAW 데이터 타입

`sqlalchemy_tibero.RAW`는 Tibero 데이터베이스에서 사용되는 바이너리 데이터 타입인 `RAW`를 나타냅니다.

**사용법**:

```python
from sqlalchemy import Column
from sqlalchemy_tibero import RAW

column = Column('my_raw_column', RAW(2000))
```

**매개변수 설명**:

- `length`: 컬럼의 최대 바이트 길이를 설정합니다. 이 값은 DDL (데이터 정의 언어) 생성 시 사용되며,
길이를 지정하지 않으면 Tibero 데이터베이스에 의해 오류가 발생합니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.RAW(length: int)
```

---

### ROWID 데이터 타입

`sqlalchemy_tibero.ROWID` 클래스는 Tibero 데이터베이스에서 사용하는 ROWID 타입을 나타냅니다.

**사용법**:

```python
from sqlalchemy import Column
from sqlalchemy_tibero import ROWID

# 만약 Tibero 데이터베이스의 물리적 주소를 나타내는 식별자를 얻고 싶다면
# 아래와 같이 사용하면 안됩니다.
column = Column('my_rowid_column', ROWID)
```

**특징**:

- ROWID는 Tibero 데이터베이스에서 각 행의 물리적 주소를 나타내는 고유 식별자입니다.
- `cast()`와 같은 함수와 함께 사용하여 ROWID 값을 생성할 수 있습니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.ROWID()
```

---

### TIMESTAMP 데이터 타입

`sqlalchemy_tibero.TIMESTAMP` 클래스는 Tibero에서 구현된 TIMESTAMP 데이터 타입을 나타냅니다.
이 데이터 타입은 Tibero 고유의 기능을 지원합니다.

**사용법**:

```python
from sqlalchemy import Column
from sqlalchemy_tibero import TIMESTAMP

# TIMESTAMP WITH TIME ZONE 데이터 타입을 사용합니다.
column = Column('my_timestamp_column', TIMESTAMP(timezone=True))

# TIMESTAMP WITH LOCAL TIME ZONE 데이터 타입을 사용합니다.
column = Column('my_timestamp_column', TIMESTAMP(local_timezone=True))

# 다음과 같은 사용은 예외가 발생합니다.
column = Column('my_timestamp_column', TIMESTAMP(timezone=True, local_timezone=True))
```

**매개변수 설명**:

- `timezone`: 이 값이 `True`로 설정되면 Tibero의 `TIMESTAMP WITH TIME ZONE` 데이터
타입이 사용됩니다. 이 타입은 시간대 정보를 포함합니다.
- `local_timezone`: 이 값이 `True`로 설정되면 Tibero의 `TIMESTAMP WITH LOCAL TIME ZONE`
데이터 타입이 사용됩니다.

**클래스 시그니처**:
```python
class sqlalchemy_tibero.TIMESTAMP(timezone: bool = False, local_timezone: bool = False)
```

## Tibero PyODBC Dialect

PyODBC 드라이버를 사용하여 Tibero 데이터베이스에 대한 지원을 제공합니다. PyODBC에 대한 문서 및 다운로드
정보는 [PyODBC GitHub](https://github.com/mkleehammer/pyodbc) 에서 확인할 수 있습니다.

### 연결하기

연결 방법에 대한 자세한 방법은 repository root 디렉토리에 있는 `sqlalchemy-tibero_install_guide.md`
문서를 참고해주세요.

### 옵션

Pyodbc를 통한 Dialect는 다음과 같은 옵션을 지원합니다. 이러한 옵션은 `create_engine()` 함수에 직접 전달됩니다.
예를 들어, 다음과 같은 형식으로 사용할 수 있습니다:

```python
engine = create_engine("tibero+pyodbc://@<DSN>", arraysize=50, char_encoding="UTF-8", wchar_encoding="UTF-8")
```

**매개변수 설명**:

- `arraysize`: statement를 실행할 cursor를 생성할 때 arraysize를 설정합니다. 자세한 설명은
[PEP-0249](https://peps.python.org/pep-0249/#arraysize)을 확인해주세요. 기본값은 50입니다.
- `char_encoding`: 데이터베이스에서 사용하는 일반 문자를 파이썬에서 디코딩할 때 사용할 인코딩을 설정합니다.
이 인코딩은 `SQL_CHAR` 유형의 데이터에 적용됩니다. 일반적으로 UTF-8 또는 다른 문자 인코딩을 지정하여
데이터의 올바른 인코딩을 보장합니다. 기본값으로 UTF-8이 사용됩니다.
- `wchar_encoding`: 데이터베이스에서 사용하는 유니코드 문자를 파이썬에서 디코딩할 때 사용할 인코딩을 설정합니다.
이 인코딩은 `SQL_WCHAR` 유형의 데이터에 적용됩니다. 주로 다국어 지원이나 유니코드 문자를 포함하는 데이터를
처리할 때 유용합니다. 기본값으로 UTF-8이 사용됩니다.


### 사용되는 환경 변수

Tibero PyODBC dialect는 Tibero ODBC 드라이버를 사용합니다. 정상적인 사용을 위해 다음과 같은
환경 변수를 사용합니다.

1. **TBCLI_WCHAR_TYPE**
   - **설명**: 이 환경 변수는 ODBC의 W로 끝나는 함수를 사용할 때 클라이언트의 문자열 집합을 UCS2로 인코딩한
   후 서버에 보낼 것을 보장합니다. Tibero Pyodbc Dialect는 `TBCLI_WCHAR_TYPE`이 설정안되어 있을 경우
   기본값으로 `UCS2`를 사용합니다.

2. **TB_NLS_LANG**
   - **설명**: Tibero 클라이언트에서 사용할 문자 집합 인코딩을 설정합니다. Tibero Pyodbc Dialect는
   `TB_NLS_LANG`이 설정안되어 있을 경우 기본값으로 `UTF8`를 사용합니다.

3. **TBCLI_COMPAT_ALCHEMY**
   - **설명**: 이 환경 변수는 Tibero의 ODBC 드라이버에서 "returning into" 기능을 가능하게 합니다.
   `TBCLI_COMPAT_ALCHEMY`이 설정안되어 있을 경우 기본값으로 `YES`를 사용합니다. 모든 Tibero ODBC
   드라이버에 적용된 환경 변수가 아니기에 Tibero PyODBC dialect 사용전에 확인이 필요합니다.

4. **TB_HOME**
   - **설명**: 이 환경 변수는 Tibero ODBC 드라이버가 사용할 Tibero Home 환경변수입니다. 이 환경변수를
   통해 드라이버가 `tbdsn.tbr` 정보를 읽을 수 있습니다. 위의 3 환경변수는 SQLAlchemy Tibero engine
   생성시 자동 설정되지만 `TB_HOME`은 설정해주지 않습니다. 이 값은 **반드시** 사용자가 설정해줘야 합니다.

**주의**: 위의 변수 설정은 반드시 `create_engine()`을 하기전에 설정해줘여 합니다. pyodbc가 `libtbodbc.so`
를 로딩해주기전에 설정을 해줘야 하게때문입니다.


### 예제

```python
import os
from sqlalchemy import create_engine

# 환경 변수 설정
# 사용자가 필요시에만 설정해주면 됩니다. 반드시 Python을 통해 설정할 필요는 없습니다. 이는 단지 예시일 뿐입니다.
os.environ.setdefault("TBCLI_WCHAR_TYPE", "UCS2")
os.environ.setdefault("TB_NLS_LANG", "UTF8")
os.environ.setdefault("TBCLI_COMPAT_ALCHEMY", "YES")

# 환경 변수 설정
# 사용자가 반드시 설정해줘야 합니다. 반드시 Python을 통해 설정할 필요는 없습니다. 이는 단지 예시일 뿐입니다.
os.environ["TB_HOME"] = "/path/to/tibero/home/directory"

# create_engine은 위의 환경 변수 이후에 있어야 합니다.
engine = create_engine("tibero+pyodbc://tibero:tmax@Tibero")
```

### 제한 사항
Tibero PyODBC Dialect는 현재 다음 기능에 대해 지원하지 않습니다.

1. 동기(sync) 및 비동기(async) 기능 (차후 기능 추가 고려중입니다)
2. 2 Phase Transactions 기능 (PyODBC 한계로 기능을 추가할 수 없습니다)
3. User Defined Type 기능 (차후 기능 추가 고려중입니다)

### 유니코드 관련 컬럼 데이터 타입
SQLAlchemy는 유니코드 데이터를 `Unicode`와 `UnicodeText` 데이터 타입으로 처리합니다. 이들은 Tibero의
`VARCHAR2`와 `CLOB` 타입에 매핑됩니다. 이를 사용하려면 데이터베이스가 유니코드를 인식할 수 있는
문자 세트로 설정되어 있어야 하며 환경 변수도 적절하게 설정되어야 합니다. 만약 데이터베이스가 유니코드 문자 세트로 설정되지
않았다면 두 가지 옵션이 있습니다.
1. **NCHAR**와 **NCLOB** 데이터 타입을 명시적으로 사용.
2. **`use_nchar_for_unicode=True`** 플래그를 `create_engine()`에 전달.
이 플래그를 설정하면 SQLAlchemy는 `Unicode`와 `UnicodeText` 타입을 `VARCHAR/CLOB` 대신
`NCHAR/NCLOB`으로 매핑합니다.

### 노트

1. SQLAlchemy와 PyODBC는 각자 독립적인 커넥션 풀(Connection Pool)을 구현하고 있습니다.
SQLAlchemy 문서에서는 두 풀을 동시에 사용하는 것을 권장하지 않습니다.
따라서, Tibero PyODBC Dialect에서는 SQLAlchemy를 사용할 때 PyODBC의
커넥션 풀을 자동으로 비활성화했습니다.

2. Tibero에서 SQLAlchemy의 `literal()`를 쓸 경우 prepared statement를 생성합니다. 이 때 타입 불일치
에러가 발생할 수 있습니다. 이 경우 `literal()`보다는 `literal_column()`를 사용해 문제를 우회할 수 있습니다.

3. SQLAlchemy `String` 타입을 사용할려면 `String(32)`처럼 숫자를 입력해야 합니다. PyODBC dialect의 경우
Tibero 내부에서 사용하는 타입은 CHAR이기 때문에 길이가 필요합니다. 만약 `VARHCAR2(30 CHAR)` 같이 CHAR를
사용하고 싶다면 `String` 타입이 아닌 sqlalchemy_tibero의 `VARCHAR` 타입을 사용해주세요.
